model_name_or_path: Qwen/Qwen3-30B-A3B
dataset_name: THUDM/LongAlign-10k
gpus_per_node: 8
trackio_project: trl-sft-benchmark
cpu_ram_efficient_loading: false

# Retry of the 32k DS-Z2 + EP=8 + chunked_nll champion (job 22099325 NCCL'd at startup).
# kernelfix = sonicmoe wrapper has no clamp; PR #45621 author landed kernel-side fix today.
# Historical baseline: 45.86 % win MFU (with clamp wrapper).

runs:
  - context_length: 32768
    nodes: 2
    backend: deepspeed_zero3
    zero_stage: 2
    dp: 16
    tp: 1
    pp: 1
    cp: 1
    ep: 8
    attn_implementation: "kernels-community/vllm-flash-attn3"
    extra_args: --enable_expert_parallel --expert_parallel_size 8 --experts_implementation sonicmoe --pad_to_multiple_of 8 --loss_type chunked_nll
    max_steps: 50
    logging_steps: 5
