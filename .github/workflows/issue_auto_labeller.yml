name: "Hugging Face Issue Labeler"
on:
  issues:
    types: opened

jobs:
  triage:
    runs-on: ubuntu-latest
    permissions:
      issues: write
    steps:
      - uses: actions/checkout@v6.0.3
      - name: Label issues
        env:
          HF_API_KEY: ${{ secrets.CI_HF_API_TOKEN }}
        run: |
          # TODO: Implement issue labeling logic directly
          # The third-party action August-murr/auto-labeler@0.0.1 has been removed
          # due to security concerns about passing secrets to unverified actions.
          echo "Issue labeling temporarily disabled - implement secure alternative"
          exit 0
