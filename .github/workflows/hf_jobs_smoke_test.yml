name: HF Jobs smoke test

on:
  workflow_dispatch:
  # TODO: temporary, remove before merging — lets the smoke test run on the PR itself
  pull_request:

jobs:
  smoke-test:
    runs-on: hf-jobs-cpu
    steps:
      - uses: actions/checkout@v4
      - run: echo "Hello from HF Jobs"
